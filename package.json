{
  "name": "answerdoctor-monorepo",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "npm run --prefix frontend dev",
    "build": "npm install --prefix frontend && npm run --prefix frontend build",
    "preview": "npm run --prefix frontend preview"
  }
}
